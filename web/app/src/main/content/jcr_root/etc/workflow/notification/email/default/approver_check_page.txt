To:

Label: ${workflow.title}
<br>
<br>

Approve or reject the following page authored by ${initiator.id}: <br>
${preview.prefix}${payload.path}.html<br><br>


The page at ${author.prefix}/editor.html${payload.path}.html will be locked <br>
until the workflow is approved or rejected.
