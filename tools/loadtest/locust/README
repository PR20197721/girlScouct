this is a load test written using the locust.io framework.  

To learn more about locust.io:
    http://locust.io/

To run the test:
    install locust io:
    > pip install locustio (python 2)
    > pip3 install locustio (python 3)
    
    > env locust.url.file=uri_list_girlscoutsatl.csv locust -f load_test.py --host=http://example.com

	locust.url.file - file which will contain uri's for load test.
	host - hostname of the site to be tested
	
Access locust console at http://127.0.0.1:8089/