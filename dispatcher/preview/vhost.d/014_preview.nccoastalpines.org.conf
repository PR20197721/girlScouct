<VirtualHost *:80>
		ServerName preview.nccoastalpines.org
		DocumentRoot /mnt/var/www/html
		ErrorDocument 404 "404 GirlScouts"

		RewriteEngine On
		RewriteLog "logs/rewrite-preview-nccoastalpines-org.log"
		RewriteLogLevel 5

		RewriteRule ^/en.html$ / [R=301,L]
		RewriteRule ^/$ /content/girlscoutsnccp/en.html [PT]

		RewriteCond %{REQUEST_URI} !^/etc(.*) [NC]
		RewriteCond %{REQUEST_URI} !^/libs(.*) [NC]
		RewriteCond %{REQUEST_URI} !^/content(.*) [NC]
		RewriteCond %{REQUEST_URI} !^/system(.*) [NC]
		RewriteCond %{REQUEST_URI} !^/dam(.*) [NC]
		RewriteCond %{REQUEST_URI} !^/services(.*) [NC]
		RewriteRule ^/(.*) /content/girlscoutsnccp/$1 [PT]


		<Directory "/mnt/var/www/html">
				<IfModule disp_apache2.c>
						SetHandler dispatcher-handler
						ModMimeUsePathInfo On
				</IfModule>

				Options FollowSymLinks
				AllowOverride AuthConfig
				Order allow,deny
				Allow from all
		</Directory>
</VirtualHost>
