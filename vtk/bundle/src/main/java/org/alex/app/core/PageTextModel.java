package org.alex.app.core;

/*
import com.cognifide.slice.mapper.annotation.JcrProperty;
import com.cognifide.slice.mapper.annotation.SliceResource;
import com.day.cq.wcm.api.PageManager;
import com.google.inject.Inject;
 
@SliceResource

*/public class PageTextModel {
 /*
    @JcrProperty
    private String text;
 
    @JcrProperty
    private String path;
 
    private final PageManager pageManager;
 
    @Inject
    public PageTextModel(PageManager pageManager) {
    	
    	System.err.println("Injetcccted");
        this.pageManager = pageManager;
    }
 
    public String getText() {
    	
    	System.err.println("getting text");
        return text;
    }
 
    public String getPageTitle() {
    	System.err.println("getPageTitle " + pageManager.getPage(path));
    	System.err.println("getPageTitle title " + pageManager.getPage(path).getTitle());
        return pageManager.getPage(path).getTitle();
    }
    */
}
